<h1>🎉 Thanks for signing this petition!</h1>
<p>
    Check out who else has
    <a href="/signers">signed the petition</a>
</p>
{{#if signature}}<img src="{{signature}}" class="signature-img" />{{/if}}

{{#if numberOfSignatures}}
    <p>Total number of signatures: {{numberOfSignatures}}</p>
{{/if}}